# Deployment checklist

1. انسخ `shop-logo.png` و`song.mp3` الأصليين من مشروعك.
2. أبقِ مجلدات `505_960`, `1000_1102`, `1150-1300` الأصلية كما هي.
3. استبدل طبقة الواجهة بالملفات الموجودة هنا.
4. غيّر رقم النسخة في `version.json` و`sw.js` عند كل إصدار.
5. شغّل الموقع عبر HTTPS أو localhost.
6. افتح DevTools > Application > Service Workers وتأكد من أن Service Worker فعال.
7. جرّب `تحديث الكاش` ثم افصل الشبكة واختبر الواجهة.
8. اختبر كل مسار من المسارات يدويًا قبل النشر.
